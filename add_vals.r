add_vals <- function(x, y){
    #' Computes the sum between two values
    #'
    #' @param x First of two values whose sum is to be calculated.
    #' @param y Second of two values whose sum is to be calculated.
    #' 
    #' @return The sum of x and y.
  return(x + y)
}